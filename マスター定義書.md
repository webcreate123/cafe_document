## マスター定義書（コード値・列挙値）


本書は、DBカラムに格納される「主なコード値（enum/定数）」を一覧化したものである。
（厳密なマスターテーブルは存在せず、主に `String` カラム＋アプリ側enumで管理する）

### 1. ロール（`users.role`）
| 名称 | 型/置き場 | 値 |
|---|---|---|
| `USER` | `cafe_api/app/core/constants.py` | `USER` |
| `STORE_OWNER` | 〃 | `STORE_OWNER` |
| `STORE_STAFF` | 〃 | `STORE_STAFF` |
| `ADMIN` | 〃 | `ADMIN` |

### 2. ユーザー状態（`users.status`）
| 名称 | 値 |
|---|---|
| `DEFAULT` | `DEFAULT` |
| `PENDING` | `PENDING` |
| `STOPPED` | `STOPPED` |
| `REJECTED` | `REJECTED` |

### 3. 店舗プラン（`stores.plan`）
| 名称 | 値 |
|---|---|
| 無料 | `STORE_PLAN_FREE` |
| プレミアム | `STORE_PLAN_PREMIUM` |

### 4. 店舗登録ステータス（`stores.registration_status`）
モデルのコメントに基づき以下を許容する。

| 名称 | 値 |
|---|---|
| pending | `pending` |
| approved | `approved` |
| rejected | `rejected` |

### 5. 問い合わせタイプ/ステータス（`inquiries`）
#### 5.1 問い合わせタイプ（`inquiries.inquiry_type`）
| 名称 | 値 |
|---|---|
| `GENERAL` | `general` |
| `STORE_REGISTRATION` | `store_registration` |
| `STORE_INQUIRY` | `store_inquiry` |
| `TECHNICAL` | `technical` |
| `BILLING` | `billing` |
| `OTHER` | `other` |

#### 5.2 問い合わせ状態（`inquiries.status`）
| 名称 | 値 |
|---|---|
| pending | `pending` |
| in_progress | `in_progress` |
| resolved | `resolved` |
| closed | `closed` |

### 6. 通知タイプ（`notifications.type`）
| 名称 | 値 |
|---|---|
| 新規店舗通知 | `new_store_nearby` |
| セール告知 | `sale_announcement` |
| 求人応募 | `job_application` |
| システム | `system` |

### 7. 通報タイプ/状態（`reports`）
#### 7.1 通報タイプ（`reports.report_type`）
| 名称 | 値 |
|---|---|
| inappropriate_content | `inappropriate_content` |
| spam | `spam` |
| fake | `fake` |
| harassment | `harassment` |
| other | `other` |

#### 7.2 通報ステータス（`reports.status`）
| 名称 | 値 |
|---|---|
| pending | `pending` |
| reviewing | `reviewing` |
| resolved | `resolved` |
| rejected | `rejected` |

### 8. 決済/サブスクリプション状態（`payments`, `subscriptions`）
| 項目 | 値 |
|---|---|
| `payments.status` | `pending`, `completed`, `failed`, `refunded` |
| `subscriptions.status` | `active`, `cancelled`, `expired` |

### 9. 重要定数（性能/運用）
| 名称 | 内容 |
|---|---|
| `RANKING_UPDATE_INTERVAL_HOURS` | `24`（ランキング更新間隔） |
| `DEFAULT_PAGE_SIZE` / `MAX_PAGE_SIZE` | `20` / `100`（ページング制御） |
| `MAX_FILE_SIZE` | `10MB`（アップロード制限） |
| `ALLOWED_IMAGE_EXTENSIONS` | `jpg/jpeg/png/gif/webp` |

